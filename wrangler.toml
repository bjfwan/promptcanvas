name = "promptcanvas"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# 本地预览：
#   npm run dev:pages       — wrangler 接管 /api/*，其余转发给 Vite dev
#   npm run preview:pages   — 构建后用 wrangler 直接服务 dist/
#
# 真实部署在 Cloudflare Dashboard 配置环境变量（OPENAI_API_KEY 等），
# 不要把密钥提交到此文件。
